---
/**
 * index.astro — home page
 *
 * Estructura:
 *   PageLayout (→ Base.astro)
 *     ├─ Hero placeholder (PR #3: HeroAbstract + HeroChoreography)
 *     └─ DemoSection (below-the-fold: Reveal + Emphasis en producción)
 *
 * Base.astro gestiona: HTML shell, fonts, global.css, motion.css,
 * Lenis init, GSAP register, grain overlay, reduced-motion guards.
 */

import PageLayout from '../layouts/PageLayout.astro'
import HeroAbstract from '../components/astro/sections/HeroAbstract.astro'
import DemoSection from '../components/astro/sections/DemoSection.astro'
---

<PageLayout
  title="Zanovix — IA aplicada a empresas"
  description="Soluciones de inteligencia artificial para empresas. Resultados medibles, implementación ética."
>
  <!-- ── Hero: SVG red de nodos + coreografía GSAP 1.5s ── -->
  <HeroAbstract />

  <!-- ── Below the fold: DemoSection valida Reveal + Emphasis en vivo ── -->
  <DemoSection />
</PageLayout>
