---
interface Props {
  as?: 'p' | 'span'
  class?: string
}

const { as: Tag = 'p', class: className = '' } = Astro.props
---

<Tag
  class:list={[
    'font-mono text-[var(--text-eyebrow)] uppercase tracking-widest',
    'text-[var(--ink-mute-strong)]',
    className,
  ]}
>
  <slot />
</Tag>
